# 《篮球女孩梦工场 · 校队联赛》需求文档（PRD / SRS）v2

> 文档类型：基于现有 Demo 的反向需求规格（As-Is Spec + To-Be 建议）+ **设计层增强（v2 新增）**
> 来源 Demo：`E:/t6/basketball-girl.html`（单文件 HTML5，原生 JS + CSS，约 5200 行，零外部依赖）
> 编制：2026-07-30（v1 功能拆解）→ 2026-07-30（v2 设计层增强）
> 适用范围：指导功能对齐、增量交付与测试校验。所有"已实现"条目均可在源文件定位到对应函数/数据。

---

## 版本变更日志（Changelog）

| 版本 | 变更 |
|---|---|
| v1 | 20 章功能拆解 + 验收清单 + 源码索引（纯 As-Is 枚举）。 |
| v2 | **新增设计层**：§1.5 设计支柱、§1.6 玩家情绪曲线、§4.2 正式核心循环文档（moment/session/long-term）、§5.6 机制规格模板、§7.6 比赛机制规格、§17.3 数值平衡表（带 `[PLACEHOLDER]` 校验态与"失衡判定"）、§21 新手引导流程、§22 失败态与边界条件总表。强化每条需求的"玩家体验目标"与"调参杠杆"。 |

> **标注约定**：`[已实现]` = 源码已落地；`[建议]` = Demo 缺失，后续迭代；`[PLACEHOLDER]` = 数值为设计假设，**未经过实测调优**，需 playtest 后回填。行号引用为撰写时快照，重构后需重新核对。

---

## 0. 文档约定

- **FR（Functional Requirement）**：编号形式列出，逐条验收。
- **模块前缀**：GROW=养成、DEV=成长发育、MATCH=比赛、LEAGUE=联赛、CUP=校队杯、WORLD=世界生态、SOC=社交关系、EVT=事件剧情、ACH=成就奖项、AWK=觉醒、END=结局、UI=界面交互、NFR=非功能、GAP=待补齐、M=机制规格。
- **玩家体验优先**：每条"已实现/建议"均补「玩家体验目标」，先讲玩家感受再讲实现。

---

## 1. 产品概述

### 1.1 产品定位
一款**女性向篮球养成模拟游戏**，融合"赛马娘式目标/羁绊"与"美少女梦工厂式志向/年度反思"双基因玩法。玩家从 12 岁初一少女起步，经 6 学年（24 季）养成与比赛，在初中→高中校队联赛与校队杯成长，终局目标是**在高中校队杯亲手击败宿敌婷婷、登顶传奇女王**。

### 1.2 目标用户
- 核心：12–28 岁、偏好二次元/养成/体育题材的女性玩家，及喜爱"数据成长曲线 + 剧情弧光"的模拟玩家。
- 次要：偏好数值经营、确定性可复现存档（种子）的硬核玩家。

### 1.3 平台与形态
- **单文件 HTML**：双击即玩，零构建、零依赖、零联网。
- 响应式：桌面（≥980px 三栏）与移动端（≤600px 单列、按钮常驻、禁双击缩放）双适配。

### 1.4 设计基调
- **像素风**：硬边描边 + 2.5px 黑描边 + 偏移投影、糖果粉蓝配色、点阵背景。
- **情绪化反馈**：比分弹窗、连携条、成就 toast、像素球场实时比分、结局综合评分。

### 1.5 设计支柱（Design Pillars）`[v2 新增]`
> 以下 5 条为**不可妥协的玩家体验**，未来任何设计决策均须对照校验。

| # | 支柱 | 非妥协体验 | 反例（违反即否决） |
|---|---|---|---|
| P1 | **成长必须可见** | 每季/每场都要让玩家"看见"自己变强（属性曲线、身高、三轴、排名）。 | 数值只后台变化、界面无反馈。 |
| P2 | **宿命对抗有弧光** | 双婷从童年战书到巅峰对决，全程可见"追平→反超→击败"弧。 | 婷婷仅作为终局 Boss 突然出现。 |
| P3 | **选择有意义且安全** | 2 选养成、策略克制、赛后交流改变走向，但不破坏数值平衡。 | 选择无差异，或选择让数值崩盘。 |
| P4 | **自然封顶不崩溃** | 无硬数值上限，靠骨龄/边际递减自然收敛，经济不通胀不崩盘。 | 出现 200+cm 或属性溢出导致比赛失真。 |
| P5 | **零摩擦即玩可验** | 单文件双击即玩；确定性种子可复现，便于自动化校验。 | 需联网/构建/手动逐帧验证。 |

### 1.6 玩家情绪曲线（24 季）`[v2 新增]`
> 设计意图：让养成不扁平，制造"落差—低谷—蜕变—登顶"的情绪过山车（对应 P2）。

| 赛季段 | 阶段 | 情绪目标 | 系统支撑 |
|---|---|---|---|
| S1–3 | 童真建立 | 轻松上手、建立养成正反馈 | 养成 2 选、首遇婷婷、梦想宣言 |
| S4–8 | 萌发与差距 | 她先窜个子 → 落差/焦虑 | `s7` 她先窜个子、婷婷领先横幅 |
| S9–13 | 低谷危机 | 挫败感（但可控） | `inSlump` ×0.6、瓶颈事件 |
| S14 | 蜕变 | 释放/逆袭爽点 | `transformBreak` 全面爆发（+技术12 等） |
| S15–18 | 追平与重逢 | 希望回升、关系修复 | `s13` 追平婷婷、`s16` 高中重逢 |
| S19–22 | 双婷对决 | 高压悬念 | `s19` 双婷对决、`s22` 巅峰前夜 |
| S23–24 | 登顶/收束 | 成就满足 | 校队杯淘汰婷婷、结局评分 |

---

## 2. 设计原则与约束

| 原则 | 说明 |
|---|---|
| 单文件零依赖 | 所有 CSS/JS 内联，不引外部库（图表手写 SVG）。 |
| 确定性可复现 | `mulberry32(seed)` 驱动世界生成；同种子→同世界，便于 jsdom 校验。 |
| 零和反馈平衡 | 开局天赋掷骰做"零和归一 + 潜力负相关"，杜绝全满/全废开局。 |
| 复合上限非硬上限 | 身高/属性靠骨龄闭合、边际递减自然封顶（仅 `stamCap` 是资源天花板）。 |
| 养成与比赛解耦 | 比赛只发短期状态与分项成长激励，不叠永久属性，防数值崩坏（P3/P4）。 |
| 增量交付逐批验收 | 每批功能完整实现并通过 jsdom 校验后再进下一批。 |

---

## 3. 系统总览与架构

### 3.1 代码分层（单文件内逻辑分层）
```
basketball-girl.html
├─ <style>              像素风设计系统（CSS 变量 + 组件类 + 动画 + 移动端媒体查询）
└─ <script>
   ├─ 工具层：clamp / mulberry32 / gauss / hashStr / sCurve / aroundN
   ├─ 数据配置层：CAT / ACTS / PAIRS / TEMPLATES / SCHOOL_TRAITS / PTRAITS /
   │              DREAMS / ACH / CHAPTERS / STRATS / TEAM_STRATS / LOCKER_EVENTS / NODES
   ├─ 世界生成层：genSchools / genRoster / assignPlayerArchetypes / genSchedule
   ├─ 养成/成长层：applyGrowth / applyOne / growBio / heightGrowthThisSeason / postSeason
   ├─ 比赛层：genGame / simScore / genBox / simTwoLeg / simBO3 / simBO5
   ├─ 赛程层：setupLeague / beginPrematch / matchContinue / startCup*
   ├─ 剧情层：rollOpener / resolveEvent / getNode / transformBreak
   ├─ 评测层：evaluateSeasonAwards / checkMilestones / computeRank
   ├─ 状态层：G（全局游戏状态单例）
   └─ 视图层：titleHTML / dreamHTML / schoolHTML / mainHTML / eventHTML /
              lockerHTML / prematchHTML / matchHTML / leagueScreenHTML /
              cupScreenHTML / endingHTML + 各类 SVG 生成器
```

### 3.2 屏幕状态机 `[已实现]`
`G.screen` 驱动整页重渲染（`render()` 按 `screen` 选 HTML 生成器）：

| screen | 含义 | 触发入口 |
|---|---|---|
| `title` | 标题屏：命名 + 选职业模板 | 启动 |
| `dream` | 梦想宣言（赛马娘式目标） | 开局 |
| `school`/`school2` | 选校（初中/高中转学） | 开局/高中进级 |
| `transfer` | 转学（复用 schoolHTML） | 高中 |
| `main` | 主养成界面（三栏） | 每季养成 |
| `event` | 事件/剧情节点 | commit 后或 postSeason |
| `locker` | 更衣室关系互动 | 随机事件 |
| `prematch` | 赛前策略 | 进入比赛 |
| `match` | 逐场交互比赛（四节 + 像素球场） | 开打 |
| `league` | 联赛结果/积分榜 | 联赛结算 |
| `cup` | 校队杯进程 | 杯赛 |
| `ending` | 结局（评分 + 三轴轨迹 + 蒙太奇） | season≥24 |

---

## 4. 核心玩法循环

### 4.1 时间轴 `[已实现]`（FR-LOOP-01）
- **6 学年 × 4 季 = 24 赛季**（`G.season` 0..23，`G.total=24`）。
- 季节：`periodName()` → 春季赛/暑期赛/秋季赛/校队杯（每学年第 4 季为杯赛季）。
- 年龄：`age = 12 + season*0.25`；`isHigh()` = 学年≥4（season≥12 进高中段）。

### 4.2 核心循环文档（Core Loop Document）`[v2 新增 · 对应 P1/P3]`

**Moment-to-Moment（0–30 秒）**
- **Action**：玩家从随机 6 张活动卡中选 **2 项**（多选机制 `G.sel` / `confirmChoice`）。
- **Feedback**：选中即预览属性增益与体力消耗；确认后属性条动画、连携 meter 填充、连携 bonus toast。
- **Reward**：即时属性增益 + 三轴（奋斗轴）累加 + 连携满足感（P1 成长可见）。
- **玩家体验目标**：每做一次选择都"看见自己变强"，养成不空洞。

**Session Loop（5–30 分钟，单季）**
- **Goal**：养成 2 项 → 事件 → 比赛 → 赛后 → 赛季结算，推动排名/关系/成就。
- **Tension**：体力与训练负荷管理（过度训练→成长效率↓）、婷婷领先差、学校成长剧本带来的格局变化。
- **Resolution**：联赛胜/负影响 `G.rank`/士气/成就；杯赛推进或淘汰；赛季末发奖项与三轴快照。

**Long-Term Loop（hours–weeks，全 24 季）**
- **Progression**：属性/身高/关系/成就/生涯目标逐季累积；学段切换引入淘汰与转学。
- **Retention Hook**：双婷宿命弧（P2）、8 项生涯目标进度、结局 S~D 评分、可收集成就图鉴（GAP-07 跨周目）。
- **玩家体验目标**：登顶传奇女王 + 亲手击败婷婷的闭环满足。

### 4.3 单季循环 `[已实现]`（FR-LOOP-02）
1. **养成选择**（`main`）：每季 6 卡选 2 项 → 属性增益/体力消耗/训练负荷。
2. **提交与事件链**（`commit`）：结算养成 → 触发本季主线/支线/随机开场 → 有比赛则进入。
3. **比赛**：联赛（32 队单循环，你队逐场交互）或校队杯（初中 2 回合 / 高中小组+淘汰）。
4. **赛后交流**（`applyPostChoice`）：3 选 1，仅影响短期状态/关系。
5. **赛季结算**（`postSeason`）：世界成长 → 自然生长 → 基础成长 → 排名 → 战力风云 → 成就复核 → 赛季奖项 → 年度反思/羁绊/矛盾 → 三轴快照 → 下一季或结局。

### 4.4 学段切换 `[已实现]`（FR-TRANSFER）
- 初中（season 0–11）`allMid`/`midTeams`；高中（season 12–23）`promoteToHigh()`：队友进级、末位淘汰（`CUT_COUNT=30`）、可选转学、国际生插班（`genIntlPlayer`）。
- 高中引入"先天特征 × 高中特性"适应性成长（契合 → 每季 +3~5%）。

---

## 5. 养成系统（Training）

### 5.1 12 项养成活动 `[已实现]`（FR-GROW-01）
映射"理论 12 轴"，每张卡含 `cat`/`ds`/`eff`/可选 `recover`/`risk`。

| 活动 | 类别 | 主要效果 |
|---|---|---|
| 营养餐 / 深度睡眠 / 营养补剂 | 营养/睡眠/微量 | 身高 +、体力 + |
| 篮球训练 / 投篮特训 / 战术研读 / 录像复盘 | 技术 | 技术/信心/学业 |
| 力量房 / 体能特训 | 力学 | 力量/体力/身高 |
| 拉伸放松 | 柔韧 | 敏捷/体力 |
| 实战比赛 / 队内对抗 | 竞赛 | 技术/人气/信心 |
| 街头斗牛 | 街球 | 人气/技术 |
| 心理辅导 / 偶像活动 / 社区义赛 | 心理/偶像 | 信心/人气 |
| 文化课 | 学业 | 学业 |
| 彻底休息 | 恢复 | 体力 +14（满血） |

**玩家体验目标**：选择即反馈，活动卡视觉化呈现"成长方向"，降低数值焦虑。

### 5.2 六对协同（连携）`[已实现]`（FR-GROW-02）
`PAIRS` 定义 11 组配对（营养×力学、训练×射术、研读×复盘…），同季同时选中有配对 2 项触发**连携加成**（额外属性增益），推动 `G.flow`（满 100 触发"连携大师"成就 `a_flow`）。

### 5.3 三轴成长面板 `[已实现]`（FR-GROW-03）
- **奋斗轴（effort）**：养成投入累计；**成果轴（result）**：胜场/数据累计；**友情轴（friend）**：关系网推导 0..100。
- 每季末 `syncAxes()` 记录 `axisLog` → 结局成长曲线 SVG。

### 5.4 体力与训练负荷 `[已实现]`（FR-GROW-04）
- `stamCap()`：随年龄 `natCap(age)`（12≈60 → 18≈100），训练扩充上限 **140**（`stamCapBonus`）。
- `growthRandom()`：体力低于上限 30%/20% 时随机增益压到 0.8–0.95 / 0.7–0.9。
- `trainLoad`：赛季累积负荷，`fatigueFactor()` 降低效率（封底 0.45）；休赛期清零。
- `EFFORT`/`FORMDELTA`/`actLoadDelta`：不同活动差异化影响状态与负荷（训练 +1.6 负荷、休息 0.4）。

### 5.5 边际递减与年龄曲线 `[已实现]`（FR-GROW-05）
- `dimRet(k,v)`：属性越高单位增益越小（受 `GROWTH_PEAK` 阈值约束）。
- `ageWin(k,age)`：低龄/青春期被动成长快、高龄趋缓。
- 休赛期"基础成长"：即使少训练也稳步提升，避免中段停滞感。

### 5.6 机制规格：养成选择 `[v2 新增 · M-GROW]`

> **Purpose**：让玩家每季主动分配有限养成资源，制造"有意义的选择"（P3）。
> **Player Fantasy**：我是教练兼主角，精打细算让自己变强。
> **Input**：从 6 张随机活动卡中勾选 2 项（`G.sel`），点"确认"。
> **Output**：2 项活动的属性增益（经 dimRet/ageWin/fatigue 折算）、体力消耗、trainLoad 增量、连携判定。
> **Success Condition**：选择落地后属性条可见变化、三轴（奋斗）累加、连携 meter 正确推进。
> **Failure State**：体力过低导致后续成长被压（growthRandom 0.7–0.9）；trainLoad 过高导致 fatigueFactor 封底 0.45（效率减半但不归零）。
> **Edge Cases**：
> - 仅选 1 项或 0 项：应禁止确认或按"彻底休息"兜底（需确认实现）。
> - 连携配对项 + 恢复项同选：连携加成是否仍触发？需明确（建议：连携只看 PAIRS 配对，与恢复项无关）。
> **Tuning Levers**：`EFFORT`（负荷）、`FORMDELTA`（状态）、`actLoadDelta`（清零/累加）、连携 `bonus` 数值、每季可选卡数（当前 6）、每季选择数（当前 2）。
> **Dependencies**：§6 成长应用、§7 比赛（体力/状态影响 databox）、GAP-01 存档。

---

## 6. 成长与发育系统（Growth & Development）

### 6.1 八项核心属性 `[已实现]`（FR-DEV-01）
`STATS = ['height','skill','stamina','strength','agility','pop','study','conf']`。
`power(s) = height*0.85 + skill*2.4 + stamina*1.0 + strength*1.05 + agility*1.1 + pop*1.7 + study*0.4 + conf*1.3`。

### 6.2 身高生长模型 `[已实现]`（FR-DEV-02）
- `heightGrowthThisSeason()`：按骨龄分层（<13 岁 7.2cm/年、<15 岁 4.8、<16 岁 2.4，之后 0.5），叠加 `GROWTH_PEAK.height=400` 复合上限。
- `boneGrowMul()`：生长板闭合（`growEnd` 骨龄）后归零——**无 200cm 硬上限，骨龄自然封顶**（P4）。
- 青春期"腿先拔节"：腿长增幅占身高增幅 ≥35%（`legShare` 约束）。

### 6.3 骨龄与发育类型 `[已实现]`（FR-DEV-03）
- `rollMaturity`/`calibrateBone`：每人骨龄增速/闭合赛季。
  - 早熟（early）：初二（s4–7）闭合，身高停摆。
  - 普通（normal）：初三~高二陆续闭合。
  - 晚熟（late）：毕业仍未闭合，全程有效增长（主角基调）。

### 6.4 开局天赋掷骰 `[已实现]`（FR-DEV-04）
`rollTalent(tplKey, r)` 每次开局重掷，零和反馈平衡：
1. **资质 `apt[k]`**：`gaussR*0.17` 偏移 ±0.30，再"零和归一"（有得必有失）→ 0.70~1.30。
2. **潜力 `pot[k]`**：`clamp(2 - apt[k] + gauss*0.06, 0.70, 1.30)` 与 `apt` **负相关**。
3. **看家本领保底**：职业关键项 `apt` ≥ 0.88，抬升额从其他项扣回。
4. **体质抽签**：78% 中上身高 + 72% 晚发育（主角基调）；晚熟 `pot.height` 额外 +0.10~0.22。

### 6.5 先天特征（PTRAITS）`[已实现]`（FR-DEV-05）
7 种特征（天才/黑马/速度型/力量型/技术型/铁人型/学霸型），影响 `gm` 成长倾向、`ovrD`/`tgtD` 战力偏移、`fit` 契合高中特性（契合 → +3~5% 适应性成长）。

### 6.6 身体档案 `[已实现]`（FR-DEV-06）
`initBio`/`growBio`/`bioStats`：身高、体重、臂展、腿长、鞋码、骨龄随季自然生长，主界面"体质档案"展示。

### 6.7 机制规格：身高/骨龄生长 `[v2 新增 · M-DEV]`
> **Purpose**：用真实发育曲线制造"晚熟反超"的爽点（P2/P4）。
> **Player Fantasy**：我的身体在悄悄变化，最终比别人高。
> **Input**：每季 `postSeason` 自动调用 `heightGrowthThisSeason()`。
> **Output**：本季身高增量（受骨龄分层 ×apt×pot×晚熟系数），写入 `G.stats.height` 与 `bio`。
> **Success Condition**：骨龄未闭合时持续增长；闭合后归零；无超出 `[PLACEHOLDER]` 合理上限的异常值。
> **Failure State**：骨龄计算错误导致闭合过早/过晚（如 early 在 s4 即停）；腿长占比 <35% 违背"腿先拔节"。
> **Edge Cases**：
> - 晚熟 + 高 apt：最终身高是否过高？（当前由 `growEnd` 19.2~20.6 骨龄 + `pot.height` 上限约束，需 playtest 验证上限）。
> - 转学前/国际生插入时骨龄重新校准是否一致。
> **Tuning Levers**：骨龄分层阈值（7.2/4.8/2.4）、`growEnd` 区间、晚熟系数（×0.72+0.8）、`GROWTH_PEAK.height=400`、`legShare` 35%。
> **Dependencies**：§5.4 体力（无直接）、§10 世界生态（对手也同语义生长）、结局身高展示。

---

## 7. 比赛引擎（Match Engine）

### 7.1 联赛自动模拟（32 队单循环）`[已实现]`（FR-MATCH-01）
`simLeague`/`genGame`/`simScore`：基于双方战力 `yE/oE` 与命中模型（`baseHit`/`interference`/`pppFromHit`），3σ 正态 `gauss` 决定波动。你队逐场**交互**进行。

### 7.2 个人数据生成 `[已实现]`（FR-MATCH-02）
`genBox(strat,win,oppStrat,forcePts)`：基于属性 + 位置模板权重（`TEMPLATE_BOX`）+ 策略倍率 + 体力惩罚（`stamina<40` 能力×0.8），生成 pts/reb/ast/stl/blk/tov/fgm/fga。**硬约束：个人得分 ≤ 全队总分**（line 2453-2454）。

### 7.3 逐场交互比赛 `[已实现]`（FR-MATCH-03）
- **赛前个人策略**（`STRATS`，6 种）：全力进攻/铁血防守/提速快攻/稳扎稳打/保存体力/针对性盯防。
- **对手克制策略**（`TEAM_STRATS`+`TEAM_STRAT_PAIRS`）：每队一套"策略配对"，开赛随机抽 1 套，玩家用对应 counter 反制。
- **四节记分牌**（`qtable`+`revealQuarter`）：逐节推进，节间可调整策略（`qadjust`）。
- **像素球场动画**：实时比分弹窗、比分 bump、解说文案。
- **伤病系统** `maybeInjury`：体力越低风险越高（<40 ×1.6，<20 ×1.9，<14 ×1.2），可休战 `resting`。

### 7.4 比赛完美数据 → 分项成长激励 `[已实现]`（FR-MATCH-04）
`applyPerfMotivation`：爆分（≥28→技术）、篮板（≥12→力量）、助攻（≥10→敏捷/人气）、抢断（≥5）/盖帽（≥4）、三双（≥3 项上双→全面 +6% 封顶）。**单项封顶 6%**，仅影响后续训练基数。

### 7.5 赛后交流 `[已实现]`（FR-MATCH-05）
`genPostChoices`/`applyPostChoice`：3 选 1，仅影响短期状态（士气/状态/体力/宿敌关系），不叠永久属性。对婷婷额外有"寒暄"选项（关系 +1）。

### 7.6 机制规格：交互比赛引擎 `[v2 新增 · M-MATCH]`
> **Purpose**：把"数值养成"兑现为"比赛爽感"，并让策略选择有意义（P3）。
> **Player Fantasy**：我在场上掌控节奏，用对策略就能赢下硬仗。
> **Input**：赛前选 `STRATS` 之一；四节间可选 `qadjust` 换策略；对手 `rollOppStrat` 随机抽 1 套。
> **Output**：四节比分（`genGame`）、个人 `genBox`、胜负、士气/状态/体力变化、成就触发、完美数据激励、伤病可能。
> **Success Condition**：比分合理（单节 ≤132、全场符合实力差）、个人≤全队、counter 策略对强敌确有胜率加成。
> **Failure State**：
> - 势头滚雪球失控 → 巨大假比分（已实现 `|差|>12` 才失衡、封顶 0.38 抑制）。
> - 体力归零���上场 → 数据被压 0.8，且伤病风险 ×1.9（可接受，属惩罚）。
> - `forcePts` 强制分数超全队 → 被 `pts>capYS` 截断（硬约束生效）。
> **Edge Cases**：
> - 加时（curY===curO）：当前随机 +1，无正式 OT 逻辑（建议：补 OT 或标注为设计取舍）。
> - 休战 `resting` 期间不触发伤病、不生成 box。
> - 对手 `activeStrat` 为 null（非交互场次）：`oppStrat` 缺省路径需确认不报错。
> **Tuning Levers**：`baseHit`/`interference`/`pppFromHit`、possessions 54~58、单节 cap 132、mom 阈值 12/封顶 0.38、`STRATS` 各倍率、`TEAM_STRATS` counter 映射。
> **Dependencies**：§5 养成（属性→box）、§6.4 天赋（apt 影响成长基数）、§11 关系（赛后）、§9 杯赛。

### 7.7 机制规格：伤病系统 `[v2 新增 · M-INJURY]`
> **Purpose**：用风险约束"无脑猛训/猛攻"，强化资源管理（P3/P4）。
> **Input**：每场 `maybeInjury(strat,win)`，读取 `strat.risk` 与当前体力。
> **Output**：`G.injury={gamesLeft, sev}`，休战 1–3 场，期间不上场。
> **Success Condition**：高风险策略（aggro risk 0.12）+ 低体力真实提高伤病率，但不致每场必伤。
> **Failure State**：体力极低仍连打 aggro → 连续伤病锁死（属玩家自招，需在引导中提示）。
> **Edge Cases**：休战中再受伤 → `if(G.injury)` 分支递减 `gamesLeft`，不叠加；低龄 <14 风险 ×1.2。
> **Tuning Levers**：`strat.risk` 各值、体力阈值（40/20/14）、伤停场数（1–3）、sev（1–2）。
> **Dependencies**：§5.4 体力、§7.3 match、赛后恢复（`POST_POOL` 冰敷 +4）。

---

## 8. 联赛与排名（League & Ranking）

### 8.1 32 队单循环 + 排名 `[已实现]`（FR-LEAGUE-01）
`computeRank` 按 `power` 对 32 队排序，`G.rank` 实时名次，含 `bestRank`/`prevRank`。主界面"联赛榜"展示趋势（up/dn）。

### 8.2 学校成长剧本 `[已实现]`（FR-LEAGUE-02）
强/中/弱三档（10/12/10）：弱队含 2 黑马+3 上升、强队 3 掉队、中游 2 黑马+5 掉队。`applySchoolGrowth(season)`：每季把 `base` 从 `base0` 线性推进到 `target`（仅本学段窗口内）。

### 8.3 本季战力风云 `[已实现]`（FR-LEAGUE-03）
`computeSeasonStory`：展示崛起/掉队球队，让成长剧本"可见可感"。

---

## 9. 校队杯（Cup）

### 9.1 初中杯：32 强两回合淘汰 `[已实现]`（FR-CUP-01）
`startCupMid`/`simTwoLeg`：玩家固定上半区 1 号位，婷婷下半区 1 号位；干跑整张签表保证双婷决赛相遇。每轮交互 2 回合（主客场）。

### 9.2 高中杯：小组赛 + 三局两胜淘汰 `[已实现]`（FR-CUP-02）
`startCupHigh`：32 进 16 小组赛（自动模拟展示玩家 3 场焦点）+ 16 强 BO3（`simBO3`，更强档 BO5 `simBO5`）。婷婷"大赛加成"保证决赛悬念。

### 9.3 双婷对决 `[已实现]`（FR-CUP-03）
固定半区结构确保玩家与婷婷必在决赛相遇；亲手淘汰婷婷解锁 `a_ting`（传奇成就）。

---

## 10. 世界与对手生态（World Dynamics）

### 10.1 32 初中 + 32 高中 `[已实现]`（FR-WORLD-01）
`genSchools(seed,isHigh)`：确定性种子生成，含校名、特性、强/中/弱档。
- **6 种学校特性**（`SCHOOL_TRAITS`）：进攻名校/铁血防守/青训基地/学术强校/全能体校/体育特长。各含 `mod`（永久养成侧重倍率）、`bonus`（入校一次性）、`offMul`/`defMul`（交手攻防）、`style`。

### 10.2 球员阵容与成长剧本 `[已实现]`（FR-WORLD-02）
`genRoster`/`assignPlayerArchetypes`：每队 8–11 人，位置结构含双后卫/双前锋/中锋。个体发育三元组（骨龄/当前身高/骨龄增速）与主角同语义。**个人成长剧本**：随机涌现"巨人中锋(>2.0m)/顶级中锋(>2.1m)/强力后卫/强力中锋"，其余平均成长（正态长尾可突破常规上限）。**晚发育个体**（全联盟 5–10%）：在校期间永不闭合，低开高走。

### 10.3 宿敌婷婷 `[已实现]`（FR-WORLD-03）
- 婷婷队锁定 `ace` 档（强度挂钩其个人成长，高于普通强队），玩家不可选。
- `tingPower()`/`balanceTing()`：主角反超/被超实时检测，触发"你反超了婷婷！"横幅。
- 主界面"宿敌对比"卡片 + 婷婷吐槽台词。

### 10.4 联盟球星涌现 `[已实现]`（FR-WORLD-04）
`computePlayerStory`：巨人中锋长成 2m+ 等高光提示（避免重复播报 `_giantNoticed`）。

### 10.5 机制规格：宿敌婷婷 `[v2 新增 · M-RIVAL]`
> **Purpose**：提供全程可见的"追平→反超→击败"目标锚（P2）。
> **Player Fantasy**：她是我命中注定的对手，我要赢过她。
> **Input**：`tingPower()` 随其个人成长推进；`balanceTing()` 每季检测领先变化。
> **Output**：宿敌对比卡片、领先/被超横幅、`relTing` 关系（-3..3）、`a_ting` 成就。
> **Success Condition**：玩家成长路径可在 s13 左右追平、s19–22 击败；婷婷强度随玩家成长动态平衡（不出现永远追不上/永远碾压）。
> **Failure State**：婷婷成长过快致玩家 24 季无法追平（需 `balanceTing` 钳制）；或过于弱势致对决无悬念。
> **Edge Cases**：玩家极端躺平 → 婷婷是否仍按 base 推进（当前按 `rivals` 线性 `gr` 推进，需确认与玩家解耦）。
> **Tuning Levers**：`ting` 初始 `offset:8`、成长率 `gr`、大赛加成、双婷决赛固定半区结构。
> **Dependencies**：§6 成长、§9.3 杯赛、§11 关系、结局评级。

---

## 11. 人物关系与社交（Relationships）

### 11.1 关系网 `[已实现]`（FR-SOC-01）
晓彤（闺蜜）、教练（恩师）、学姐（憧憬）、后辈（榜样）、婷婷（宿敌，`flags.relTing` -3..3）。各关系 0..100，主界面"好友关系面板"进度条。

### 11.2 更衣室关系互动 `[已实现]`（FR-SOC-02）
`LOCKER_EVENTS`（8 种��景，正/负/混合）：队友动作含固有效果 + 2–3 回应选项（影响关系/状态/友情轴）。如"并肩作战""对抗中的肘子""让出首发""社媒抢风头""更衣室流言"。

### 11.3 羁绊阈值与矛盾弧 `[已实现]`（FR-SOC-03）
- `fireBondOrYear`：关系跨档触发羁绊阈值事件（每档一次）。
- `conflictDue`：关系熟络触发一次矛盾→化解弧（每段最多一次）。
- 均在 `postSeason` 可靠出口触发，保证中段必有摩擦与和解。

---

## 12. 事件与剧情系统（Events & Story）

### 12.1 双婷主线（8 章）`[已实现]`（FR-EVT-01）
`CHAPTERS`+`SPINE`：童年战书(s1)→初中首遇(s4)→她先窜个子(s7)→瓶颈低谷(crisis)→追平婷婷(s13)→高中重逢(s16)→双婷对决(s19)→巅峰前夜(s22)。主线界面 stepper 可见。

### 12.2 三条支线与随机开场 `[已实现]`（FR-EVT-02）
`NODES` 事件树（line 3116 起，约 320 行）：主线 + 支线（晓彤/教练/婷婷）+ 低谷危机 + 生日/伤病/偶像/转学等随机开场（`OPENERS`/`BRANCH_OPENERS`）。节点含 `eff`/`choices`/`next`/`_gate`/`_peace`。

### 12.3 低谷危机与蜕变 `[已实现]`（FR-EVT-03）
- 13–15 岁（season 9–13）`inSlump` 低谷期，基础成长 ×0.6。
- `transformBreak`（season 14）：危机已处理则全面爆发（技术+12 等），否则缓慢恢复。

### 12.4 梦想宣言与性格萌芽 `[已实现]`（FR-EVT-04）
`DREAMS`（5 种）：开局选 1，给初始属性倾向 + 影响结局标题权重。`persona`（passion/calm/warm）：由养成选择累积，影响事件分支与结局。

### 12.5 年度反思 `[已实现]`（FR-EVT-05）
`yearReflect`：每跨学年在 `postSeason` 直接回望一次（可靠出口，杜绝被事件吞掉）。

---

## 13. 成就与奖项（Achievements & Awards）

### 13.1 成就体系 `[已实现]`（FR-ACH-01）
`ACH`（21 项：初露锋芒/单场30分/三双之夜/校队杯夺冠/宿敌终结/传奇女王…）+ `ACH_META`（图标/稀有度 bronze-silver-gold-legend/描述）。解锁弹 toast（按稀有度配色）。

### 13.2 赛季奖项 `[已实现]`（FR-ACH-02）
`evaluateSeasonAwards`：每季末评 **MVP** 与 **进步奖**（覆盖属性/士气/成就三轴向奖励），记 `G.seasonAwards`。

### 13.3 生涯目标进度 `[已实现]`（FR-ACH-03）
`CAREER_GOALS`（8 项：单场30分/联赛登顶/校队杯夺冠/超越婷婷/赛季MVP/全明星/更衣室之魂/传奇女王），主界面实时进度条。

---

## 14. 才能开花 / 觉醒（Awakenings）

### 14.1 觉醒时刻 `[已实现]`（FR-AWK-01）
`awakenings`/`awakened`：特定阈值（羁绊跨档、传承/榜样）触发"才能开花"弹窗（脉冲动画），记结局蒙太奇回扣。

### 14.2 矛盾节点 `[已实现]`（FR-AWK-02）
`conflictFired`：每段关系最多一次矛盾节点，化解后入 `peaceLog`（结局"摩擦→和解"弧）。

---

## 15. 结局系统（Ending）

### 15.1 综合评分 `[已实现]`（FR-END-01）
结局综合评分 S/A/B/C/D（`grade-card` 配色 + 大字徽章），基于生涯数据（胜场、成就、属性、关系、梦想达成、觉醒、克服挫折）加权。

### 15.2 三轴轨迹 + 蒙太奇回扣 `[已实现]`（FR-END-02）
结局展示三轴（奋斗/成果/友情）成长曲线 SVG（`axisTrajectorySVG`）；蒙太奇回扣汇总梦想/羁绊/觉醒/挫折/反思记忆点。

---

## 16. 界面与交互（UI / UX）`[已实现]`

### 16.1 像素风设计系统 `[已实现]`（FR-UI-01）
CSS 变量统一配色；2.5px 黑描边 + 偏移投影；点阵/条纹背景；按钮 `:active` 下沉。

### 16.2 主界面三栏布局 `[已实现]`（FR-UI-02）
`grid: 310px 1fr 300px`：左（头像/体质档案/雷达/三轴）、中（养成 6 卡/宿敌对比/联赛榜/生涯目标/章节）、右（好友关系/战力风云/球星/像素外景）。≤980px 单列。

### 16.3 反馈与动画 `[已实现]`（FR-UI-03）
成就 toast、比分弹窗、连携条、雷达图、赛季数据图（`statsChartSVG`）、四节记分牌高亮当前节。

### 16.4 移动端适配 `[已实现]`（FR-UI-04）
≤600px：单列、标题按钮 sticky 常驻底部、输入框 ≥16px 防 iOS 缩放、网格降列、禁 300ms 延迟与双击缩放。

---

## 17. 数值与平衡设计（Balance）

### 17.1 随机性与可复现 `[已实现]`
- `mulberry32(seed)`：确定性 PRNG，同种子→同世界（便于 jsdom 校验）。
- `gauss`/`gaussR`：Box-Muller 正态，用于身高/腿长/天赋长尾、3σ 比分（允许罕见超常个体，不截断上限）。
- `hashStr`：FNV 哈希，将校名稳定映射为种子/特性/策略配对。

### 17.2 关键公式速查 `[已实现]`
| 公式 | 位置 | 说明 |
|---|---|---|
| `power(s)` | line 1714 | 综合战力加权求和 |
| `natCap`/`stamCap` | 598–599 | 体力上限随年龄 60→100，训练扩至 140 |
| `dimRet`/`ageWin` | 1983–1995 | 边际递减 + 年龄成长曲线 |
| `heightGrowthThisSeason` | 1703 | 骨龄分层身高增长 |
| `baseHit`/`interference`/`pppFromHit` | 2172–2175 | 命中模型与回合得分 |
| `genBox` | 2444 | 个人数据生成 + 个人≤全队约束 |

### 17.3 数值平衡表（Economy/Balance Spreadsheet）`[v2 新增]`
> 填写规则：所有数值当前为**设计假设**，标注 `[PLACEHOLDER]` 表示**未经实测调优**，playtest 后回填真实值。每格附 rationale（无 magic number）。

| 变量 | Base | Min | Max | 校验态 | Rationale / 调参说明 |
|---|---|---|---|---|---|
| 体力上限 `natCap(age)` | 60@12→100@18 | 60 | 100 | `[PLACEHOLDER]` | 训练可 +`stamCapBonus` 至 140；峰值是否够需实测 |
| `growthRandom` 正常 | 0.70~1.40 | 0.70 | 1.40 | `[PLACEHOLDER]` | 体力充足时的随机增益浮动 |
| `growthRandom` <30%上限 | 0.80~0.95 | 0.80 | 0.95 | `[PLACEHOLDER]` | 体力长期过低惩罚 |
| `growthRandom` <20%上限 | 0.70~0.90 | 0.70 | 0.90 | `[PLACEHOLDER]` | 更严重惩罚 |
| 身高年增长（骨龄<13） | 7.2 cm/年 | — | — | `[PLACEHOLDER]` | 童年缓长 |
| 身高年增长（<15） | 4.8 cm/年 | — | — | `[PLACEHOLDER]` | 青春猛长 |
| 身高年增长（<16） | 2.4 cm/年 | — | — | `[PLACEHOLDER]` | 趋缓 |
| 晚熟系数 | ×0.72+0.8 | — | — | `[PLACEHOLDER]` | 晚熟单季更平缓但更长 |
| `baseHit(e)` | 0.28~0.70 | 0.28 | 0.70 | `[PLACEHOLDER]` | `clamp(0.45+(e-55)/300,...)` 实力越强越高 |
| `interference(e)` | 0~0.20 | 0 | 0.20 | `[PLACEHOLDER]` | `(e-55)/420` 防守干扰 |
| `pppFromHit(eh)` | ≈1.89@eh0.45 | 0.5 | 4.2 | `[PLACEHOLDER]` | `eh*3.2+0.45`，吻合 24 秒节奏 |
| 全场进攻次数 `youPossT` | 强58/弱54 | 54 | 58 | `[PLACEHOLDER]` | 高强度节奏 |
| 单节得分上限 | 132 | — | 132 | `[PLACEHOLDER]` | 含 2+1 罚球，防比分爆炸 |
| 势头阈值 `mom` | 差>12 触发 | 0 | 0.38 | `[PLACEHOLDER]` | `|差|>12` 才失衡，封顶 0.38 抑滚雪球 |
| `STRATS.aggro.winBonus` | +0.01 | — | — | `[PLACEHOLDER]` | 进攻策略胜率加成 |
| `STRATS.defense.winBonus` | +0.05 | — | — | `[PLACEHOLDER]` | 防守策略胜率加成 |
| `STRATS.focus.winBonus`（对强敌） | +0.06 | — | — | `[PLACEHOLDER]` | 针对性盯防克制婷婷/强敌 |
| `STRATS.*.staminaCost` | 5~9（save -4） | -4 | 9 | `[PLACEHOLDER]` | 影响体力消耗与伤病风险 |
| 完美数据：爆分阈值 | pts≥28 | — | — | `[PLACEHOLDER]` | → 技术成长激励 |
| 完美数据：篮板阈值 | reb≥12 | — | — | `[PLACEHOLDER]` | → 力量 |
| 完美数据：助攻阈值 | ast≥10 | — | — | `[PLACEHOLDER]` | → 敏捷/人气 |
| 完美数据：抢断/盖帽 | stl≥5/blk≥4 | — | — | `[PLACEHOLDER]` | → 敏捷/力量 |
| 完美数据激励单项封顶 | 6% | — | 0.06 | `[PLACEHOLDER]` | `CAP=0.06`，仅影响训练基数 |
| 伤病风险倍率（体力<40） | ×1.6 | — | — | `[PLACEHOLDER]` | 低体力更易伤 |
| 伤病风险倍率（体力<20） | ×1.9 | — | — | `[PLACEHOLDER]` | 极低体力 |
| 伤病风险倍率（年龄<14） | ×1.2 | — | — | `[PLACEHOLDER]` | 低龄更脆 |
| 伤停场数 | 1–3 场 | 1 | 3 | `[PLACEHOLDER]` | `1+floor(rng*3)` |
| 天赋 `apt` 区间 | 0.70~1.30 | 0.70 | 1.30 | `[PLACEHOLDER]` | 零和归一后范围 |
| 天赋 `pot` 区间 | 0.70~1.30 | 0.70 | 1.30 | `[PLACEHOLDER]` | 与 apt 负相关 |
| 看家本领保底 `apt` | ≥0.88 | — | — | `[PLACEHOLDER]` | 职业关键项下限 |
| 晚熟概率 | 72% | — | — | `[PLACEHOLDER]` | 主角基调设定 |
| 中上身高概率 | 78% | — | — | `[PLACEHOLDER]` | 体质抽签 |
| `GROWTH_PEAK`（skill/strength/...） | 85/95/80/120/65/75/90/400 | — | — | `[PLACEHOLDER]` | 各属性边际递减阈值 |
| `AGE_WIN` 峰值年龄（skill/agility/...） | 14.0/13.5/15.5/... | — | — | `[PLACEHOLDER]` | 高斯钟形成长窗口 |
| `fatigueFactor` 封底 | 0.45 | 0.45 | 1.0 | `[PLACEHOLDER]` | `clamp(1-trainLoad/220,0.45,1)` |
| `actLoadDelta`（休息类） | -45 | -45 | — | `[PLACEHOLDER]` | 训练负荷清零 |
| `CUT_COUNT`（高中末位淘汰） | 30 | — | — | `[PLACEHOLDER]` | 进级淘汰人数 |
| `inSlump` 基础成长 | ×0.6 | — | — | `[PLACEHOLDER]` | season 9–13 低谷 |
| `transformBreak` 爆发（已处理） | 技术+12 等 | — | — | `[PLACEHOLDER]` | `{height2.5,skill12,strength9,agility8,conf9,stamina6,pop5}` |

### 17.4 如何判定"失衡"（Define Broken First）`[v2 新增]`
> playtest 前先定义失败标准，避免"感觉不对"式模糊反馈。

| 系统 | 失衡判定（Broken） | 健康信号 |
|---|---|---|
| 比赛 | 单场分差 >60 且无实力差支撑；或个人得分 > 全队 | 分差与战力差正相关，box 合理 |
| 养成 | 24 季后属性溢出（如 skill>200）或某属性锁死不涨 | 属性在 GROWTH_PEAK 附近自然收敛 |
| 身高 | 出��� >205cm 或骨龄闭合异常早（early 在 s4 停） | 晚熟反超、终局 170–195cm 区间 |
| 经济 | 连携/完美数据让某项增速长期 >10%/季 | 单项封顶 6%，长尾可控 |
| 宿敌 | 玩家 24 季无法追平婷婷，或婷婷毫无悬念 | s13 附近可追平，s19–22 可击败 |
| 体力 | 任意策略可无代价连打不伤、不降效 | 低体力确有惩罚（成长/数据/伤病） |

---

## 18. 非功能性需求（NFR）

| 编号 | 需求 | 状态 |
|---|---|---|
| NFR-01 | 单文件、零外部依赖、双击即玩 | `[已实现]` |
| NFR-02 | 确定性种子可复现（同种子同世界） | `[已实现]` |
| NFR-03 | 移动端响应式（≤600px 可玩） | `[已实现]` |
| NFR-04 | 整页重渲染（无虚拟 DOM，状态驱动） | `[已实现]` |
| NFR-05 | 性能：单季渲染/计算毫秒级无卡顿 | `[已实现]`（建议补压测） |
| NFR-06 | 可测试性：关键逻辑纯函数化，便于 jsdom 注入种子 | `[部分]`（需补显式测试入口） |

---

## 19. 已知缺口与迭代路线（GAP / To-Be）

| 编号 | 缺口 | 建议 | 优先级 |
|---|---|---|---|
| GAP-01 | **无存档/读档**：`newGame` 后 `G` 仅内存，刷新即丢 | `localStorage` 序列化（JSON.stringify/parse），含"继续游戏/新游戏" | **P0（第一迭代）** |
| GAP-02 | **无音效/音乐**：纯静音 | WebAudio 轻量音效（点击/得分/成就）+ 静音开关 | P2 |
| GAP-03 | **无显式测试钩子** | 暴露 `window.__game` + 种子回归用例（seeds 1/8/16/42/99/256/512/888） | P1 |
| GAP-04 | **国际生仅高中段** | 评估初中段国际生机制或标注设计取舍 | P3 |
| GAP-05 | **结局分支有限** | 基于梦想/性格/关系派生 4–6 种结局标题与专属文案 | P2 |
| GAP-06 | **无难度/重开选项** | 难度档（休闲/普通/硬核）影响低谷深度与婷婷强度 | P2 |
| GAP-07 | **成就无跨周目图鉴** | 跨周目成就图鉴（localStorage）作收集要素 | P3 |
| GAP-08 | **无多人/分享** | 导出"生涯卡片"图片分享（canvas 截图） | P3 |

---

## 20. 验收清单（摘录）

- [ ] 开局：命名 + 三模板 + 梦想宣言 + 天赋掷骰（零和平衡）可玩。
- [ ] 24 季循环：每季 2 项养成 → 事件 → 比赛 → 赛后 → 结算 完整闭环。
- [ ] 养成：12 活动 + 11 连携 + 三轴 + 体力/负荷 + 边际递减 生效。
- [ ] 发育：身高骨龄曲线、早/晚熟、先天特征、晚发育反超 可见。
- [ ] 比赛：联赛自动模拟 + 逐场交互四节 + 个人≤全队约束 + 伤病。
- [ ] 杯赛：初中 2 回合 / 高中小组+BO3/BO5，双婷决赛必相遇。
- [ ] 世界：32 初中/高中 + 6 特性 + 成长剧本 + 婷婷生态。
- [ ] 社交：关系网 + 更衣室 8 事件 + 羁绊/矛盾弧。
- [ ] 剧情：双婷 8 章 + 支线 + 危机蜕变 + 年度反思。
- [ ] 成就：21 项 + 赛季 MVP/进步奖 + 生涯目标进度。
- [ ] 结局：S~D 评分 + 三轴轨迹 + 蒙太奇回扣。
- [ ] UI：像素风 + 三栏 + 移动端适配 + 反馈动画。
- [ ] 同种子可复现（jsdom 校验通过）。
- [ ] **[v2]** 设计支柱 P1–P5 在对应界面/系统均有可观察的体现。
- [ ] **[v2]** 数值平衡表所有 `[PLACEHOLDER]` 经一轮 playtest 回填。

---

## 21. 新手引导流程（Onboarding Flow）`[v2 新增 · 对应 P1/P3]`

> 原则：核心动词 30 秒内出现；首胜可期；新机制低风险引入；至少一个机制靠探索发现；首 session 以钩子收尾。

- [ ] **核心动词 30 秒引入**：标题屏命名→选模板→首季即出现"选 2 活动"与"比赛交互"两个核心动词。
- [ ] **首胜保证（建议）**：第一场联赛对手强度下调，确保教程 beat 1 不会失败（当前未强制保证，建议 `G.season===0` 时 `pa` 偏向玩家）。
- [ ] **机制低风险引入**：事件/赛后交流均为"无惩罚选择"；连携、策略克制在已熟悉养成后引入。
- [ ] **探索式发现**：连携系统不靠文字教学，玩家同时选 PAIRS 配对项时自发展示 bonus（"发现感"）。
- [ ] **首 session 钩子收尾**：第一章"童年战书"+ 梦想宣言 + 婷婷初遇，留下"下次要追上她"的"one more"驱动。
- [ ] **引导不阻断**：所有提示为非模态 toast/高亮，可跳过。

---

## 22. 失败态与边界条件总表（Edge Cases & Failure States）`[v2 新增]`

| 系统 | 边界/失败场景 | 当前处理 | 风险/待确认 |
|---|---|---|---|
| 养成 | 选 0/1 项即确认 | 需确认是否禁止 | 可能空转一季 |
| 养成 | 体力极低 → 成长被压 | growthRandom 0.7–0.9 | 符合预期 |
| 连携 | 连携项 + 恢复项同选 | 仅看 PAIRS 配对 | 建议文档明确 |
| 身高 | 晚熟+高 apt 终值过高 | growEnd 骨龄约束 | 需 playtest 上限 |
| 比赛 | 加时（平分） | 随机 +1，无 OT | 建议补 OT 或标注取舍 |
| 比赛 | forcePts > 全队 | `pts>capYS` 截断 | 硬约束生效 |
| 比赛 | 休战期再受伤 | `if(G.injury)` 递减 | 不叠加，OK |
| 比赛 | 非交互场 oppStrat=null | 缺省路径 | 需确认不报错 |
| 伤病 | 低体力连打 aggro | 风险 ×1.9 | 玩家自招，需引导提示 |
| 天赋 | 全满/全废开局 | 零和归一 + 保底 | 已杜绝 |
| 宿敌 | 玩家躺平仍追不上 | rivals 线性推进 | 需 balanceTing 钳制 |
| 学段 | 转学/国际生骨龄重校 | initBio/growBio | 需一致性校验 |
| 存档 | 刷新丢失 | 无（GAP-01） | **P0 优先补** |
| 测试 | 无显式钩子 | NFR-06 部分 | GAP-03 P1 |

---

## 附录 A：源文件结构索引（行号快照）

| 范围 | 内容 |
|---|---|
| 1–589 | `<style>` 像素风设计系统（含移动端媒体查询） |
| 593–615 | 工具函数（clamp/mulberry32/gauss） |
| 616–794 | 养成/天赋/成就/目标/章节/梦想 数据配置 |
| 798–987 | 学校特性/先天特征/发育类型/阵容生成 |
| 990–1181 | 世界生成（genSchools/assignPlayerArchetypes） |
| 1185–1380 | 成长剧本/球员成长/进级 |
| 1380–1583 | 转学/国际生/队友进级 |
| 1585–1717 | newGame / 身体 / 战力 / 发育系数 |
| 1719–2090 | 成长应用 / 比赛经验 / 提交养成 |
| 2090 起 | 联赛模拟 / 交互比赛引擎 / 策略 / 杯赛 |
| 2242–2280 | STRATS / TEAM_STRATS / 策略配对（数值源） |
| 2444–2510 | genBox / 完美数据激励 / 伤病（数值源） |
| 2758–3030 | 校队杯 / 赛程 / 结算 / 奖项 |
| 3027–3115 | postSeason / transformBreak |
| 3116–3460 | 事件树 NODES（主线/支线/危机） |
| 3460–3540 | 事件分发 / 觉醒 / 羁绊 |
| 3540–3932 | 渲染辅助 / genOffer |
| 3932–5089 | 各屏幕 HTML 生成器 |
| 5089–5212+ | render 调度 / 入口 / 全局对象导出 |

> 注：本需求文档是对现有 Demo 的忠实逆向拆解 + v2 设计层增强；标注 `[建议]`/`[PLACEHOLDER]` 的条目为后续迭代机会或待调优假设，不视为当前缺陷。
